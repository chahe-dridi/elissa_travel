<form method="post" action="{{ path('app_volclass_delete', {'id': volclass.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ volclass.id) }}">
    <button class="btn">Delete</button>
</form>
