{% extends 'nav_admin.html.twig' %}

{% block body %}
<div class="container-fluid">
    <!-- Row 1 -->
    <div class="row">
        <div class="col-lg-8">
            <h1>Create new Hotel</h1>

            {{ include('hotel/_form.html.twig') }}

            <a href="{{ path('app_hotel_index') }}" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
</div>
{% endblock %}
