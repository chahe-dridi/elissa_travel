<form method="post" action="{{ path('app_vol_delete', {'id': vol.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vol.id) }}">
    <button class="btn">Delete</button>
</form>
